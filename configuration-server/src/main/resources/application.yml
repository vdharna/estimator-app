server:
  port: 8888
spring:
  cloud:
    config:
      discovery:
        enabled: true
      server:
        encrypt.enabled: false
        git:
          uri: https://github.com/vdharna/estimator-app
          searchPaths: config-repo/project-service, config-repo/organization-service
          username: # -Dspring.cloud.config.server.git.username
          password: # -Dspring.cloud.config.server.git.password