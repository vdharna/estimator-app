spring:
  application:
    name: organization-service
  
  cloud:
    config:
      enabled: false

---

spring:
  profiles: cloud
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888
      fail-fast: true
      

  

